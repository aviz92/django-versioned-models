[pytest]
log_cli = true
log_cli_level = DEBUG
testpaths = tests
pythonpath = .
DJANGO_SETTINGS_MODULE = tests.settings

addopts =
;    --collect-only
    -s
    -v
    -ra
    --strict-markers
    --tb=short

    --better-report


markers =
    example: Marker for tests example.
