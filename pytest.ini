[pytest]
log_cli = true
log_cli_level = DEBUG
testpaths = tests
pythonpath = . django_versioned_models

addopts =
;    --collect-only
    -s
    -v
    -ra
    --strict-markers
    --tb=short

    --better-report


markers =
    example: Marker for tests example.
